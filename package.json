{
  "name": "ui-test-assessment",
  "version": "1.0.0",
  "description": "Web-based application that facilitates authenticated access for employee information. This interface allows employees to review and interact with various data points such as names, job titles, and geographic origins.",
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "start": "ts-node server.ts",
    "cy:run": "cypress run --config-file \"cypress.config.ts\"",
    "cy:run:ui": "cypress open --e2e --config-file \"cypress.config.ts\"",
    "test:format": "prettier --check \"cypress/**/*\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.3",
    "cypress": "^13.6.1",
    "prettier": "3.1.0",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
